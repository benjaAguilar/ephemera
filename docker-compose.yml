services:
  db:
    image: postgres:18
    env_file:
      - .env
    volumes:
      - postgres_data_prod:/var/lib/postgresql

  app:
    build: .
    ports:
      - 3000:3000

volumes:
  postgres_data_prod:
    name: ephemera_prod_db
